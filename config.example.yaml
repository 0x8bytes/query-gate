server:
  port: 8080
  query_timeout: 30s
  max_rows: 10000
  log_level: info

auth:
  ip_whitelist:
    - "*"            # * = 允许所有 IP；生产建议改为具体 IP 列表
  jwt_secret: ""     #生产建议: openssl rand -hex 32

storage:
  sqlite_path: /data/querygate.db

log:
  retention_days: 30
